# 贡献 LangGraph

感谢您对贡献 LangGraph 感兴趣！

## 通用指南

以下是一些您在所有类型贡献中需要牢记的事项：

- 遵循 ["fork and pull request"](https://docs.github.com/en/get-started/exploring-projects-on-github/contributing-to-a-project) 工作流程。
- 在提交 Pull Request 时，请填写已包含的 Pull Request 模板。注意相关的 issue 并且标记相关的维护者。
- 在请求审查之前，请确保您的 PR 通过了代码格式化、代码风格检查和测试。
  - 如果您需要评论或反馈，请标记一位维护者。
- 向后兼容性至关重要。您的更改不应具有破坏性，除非是关键的 bug 和安全修复。
- 在提交新的 PR 或 issue 之前，请先查找是否已有重复的 PR 或 issue。
- 尽量使更改的范围保持隔离。通常来说，您的更改不应一次影响多个包。

### Bug 修复

对于 Bug 修复，请先提交一个 issue，然后我们再提出修复方案，以确保该方案能正确解决根本问题。一般来说，Bug 修复都应附带一个单元测试，该测试在修复前会失败。

### 新功能

对于新功能，请先在 [discussion](https://forum.langchain.com/) 中开启一个新帖子，维护者将帮助您规划所需进行的更改。

## 贡献文档

文档是 LangGraph 的重要组成部分。我们欢迎为新功能编写新文档，也欢迎社区改进我们现有的文档。在开始之前，请阅读以下资源：

- [文档风格指南](#documentation-style-guide)
- [文档设置](#setup)

## 文档风格指南

随着 LangGraph 的不断发展，其所需文档的广度也在不断扩大。
本文档为所有为 LangGraph 编写文档的人员提供指导，以及我们围绕组织和结构的一些理念。

## 理念

LangGraph 的文档遵循 [Diataxis 框架](https://diataxis.fr)。
在该框架下，所有文档都归入以下四类之一：[教程](#tutorials)、
[操作指南](#how-to-guides)、
[参考](#references) 和 [解释（也称概念指南）](#conceptual-guide)。

### 教程

教程是通过一个实际操作来引导读者的课程。它们的目的是通过展示一种实现某个目标的方法，帮助用户理解概念及其交互方式。

它们应该**避免**深入介绍实现该目标的多种不同方式。选择本身就很麻烦。相反，它们应该引导新用户按照推荐的路径来完成一个具体的目标。虽然教程的最终结果不一定需要完全达到生产就绪状态，但它应该是实用的，并且能够很好地满足教程开头明确陈述的目标。

引用 Diataxis 网站上的话：

> 教程服务于用户对技能和知识的*获取*——他们的学习。其目的不是帮助用户完成某项工作，而是帮助他们学习。

在 LangGraph 中，这些通常是更高级别的指南，展示端到端的用例。

一些例子包括：

- [构建一个客户支持机器人](https://langchain-ai.github.io/langgraph/tutorials/customer-support/customer-support/)
- [构建一个 SQL Agent](https://langchain-ai.github.io/langgraph/tutorials/sql/sql-agent/)

以下是一些撰写优秀教程的高级技巧：

- 专注于引导用户完成某项工作，但请记住，最终目标是传授原理，而不是创建一个完美的生产系统。
- 具体，而非抽象；遵循一条路径。
  - 无需深入探讨替代方案，但可以引用它们，最好是链接到适当的操作指南。
- 尽快“取得进展”——让用户能够运行一些代码并看到输出。
  - 之后您可以进行迭代和扩展。
  - 尽量在用户可以运行代码并看到进度的关键步骤设置检查点。
- 关注结果，而非技术解释。
  - 大量链接到合适的概念/参考页面。
- 首次提及 LangGraph 概念时，使用其全称（例如，“human-in-the-loop”），并链接到其概念/其他文档页面。
  - 添加一个前提条件提示，链接到任何包含必要背景信息的页面也很有帮助。
- 以总结/后续步骤部分结束，总结教程内容并提供未来阅读方向，例如相关的操作指南。
- 使用“接下来，我们可以运行 X 和 Y。我们预计会得到 Z。”之类的短语。然后，使用“注意 Z”之类的语言来回顾我们的预期，并将读者的注意力引向我们试图教授的主题。
- 不要害怕重复。

### 操作指南

顾名思义，操作指南演示如何执行特定、独立的操作。
它假定用户已经熟悉底层概念，并且尝试解决一个直接的问题，但
仍应提供一些背景信息或列出其中信息可能适用的场景。
在某些情况下，它们可以并且应该讨论替代方案，因为一种方法可能比另一种方法更好。

引用 Diataxis 网站上的话：

> 操作指南服务于已具备能力的用户的事务。您可以假设他们知道自己想要做什么，并且能够正确地遵循您的指示。

一些例子包括：

- [如何为您的图添加持久化](https://langchain-ai.github.io/langgraph/how-tos/persistence/)
- [如何查看和更新过去的图状态](https://langchain-ai.github.io/langgraph/how-tos/human_in_the_loop/time-travel/)

以下是一些撰写优秀操作指南的高级技巧：

- 在开头清楚地解释您将要指导用户完成什么。
- 假定比教程更高的意图，并展示用户需要做什么才能完成该任务。
- 假定熟悉概念，但解释为什么建议的操作会有帮助。
  - 大量链接到概念/参考页面。
- 讨论在解决问题时可能出现的权衡的替代方案和响应。
- 使用大量示例代码，最好将它们放在完整的代码块中，读者可以直接复制运行。
- 以总结/后续步骤部分结束，总结教程内容并提供未来阅读方向，例如其他相关的操作指南。

### 概念指南

LangGraph 的概念指南属于 Diataxis 框架的**解释**部分。它们应该以比操作指南或教程更抽象的方式涵盖 LangGraph 的术语和概念，并且面向对深入理解该框架感兴趣的求知欲强的用户。尽量避免过于庞大的代码示例。这里的目标是为用户提供视角，而不是完成一个实际项目。这些指南应该涵盖事物**为何**以这种方式工作。

引用 Diataxis 网站上的话：

> 解释的视角比其他类型都更高更广。它不像操作指南那样从用户的视角来看，也不像参考资料那样近距离观察机器。它的范围是“一个主题”——一个“知识领域”，这个领域在某种程度上需要得到合理且有意义的界定。

一些例子包括：

- [什么是“Agentic”？](https://langchain-ai.github.io/langgraph/concepts/high_level/)
- [工具调用](https://langchain-ai.github.io/langgraph/concepts/agentic_concepts/#tool-calling)

以下是一些撰写优秀概念指南的高级技巧：

- 解释设计决策。概念 X 为什么存在，以及为什么这样设计？
- 使用类比和引用其他概念与替代方案。
- 避免混入过多的参考内容。
- 您可以并且应该引用其他指南中涵盖的内容，但请确保链接到它们。

### 参考

参考资料包含详细的底层信息，精确描述了存在哪些功能以及如何使用它们。
在 LangGraph 中，这主要是我们的 API 参考页面，这些页面是从代码中的文档字符串（docstrings）生成的。
参考页面通常不是逐字阅读的，而是在用户需要了解如何使用特定功能时进行查阅。

引用 Diataxis 网站上的话：

> 参考指南的唯一目的是尽可能简洁、有序地进行描述。而教程和操作指南的内容由用户的需求驱动，参考资料则由其描述的产品驱动。

LangChain 中的许多参考页面都是从代码中自动生成的，
但以下是一些撰写优秀文档字符串的高级技巧：

- 保持简洁。
- 讨论特殊情况和与用户期望的偏差。
- 详细说明必需的输入和输出。
- 简要说明何时可能使用该功能是可以的，但深入的细节应放在其他部分。

每个类别都服务于不同的目的，并需要特定的编写和组织内容的方法。

## 通用指南

在编写和组织文档时，您还应考虑其他一些指南。

我们通常不会合并来自外部贡献者的新教程，除非有实际需求。
我们欢迎对现有集成文档、操作指南和参考资料的更新和补充。

### 避免重复

在多个页面上深入介绍相同内容会难以维护并引起混淆。对于特定的概念或功能，应该只有一个（极少数情况下是两个）规范的页面。取而代之的是，您应该链接到其他指南。

### 链接到其他部分

由于文档的各个部分并非孤立存在，因此尽可能多地链接到其他部分非常重要，以便开发人员可以内联了解不熟悉的主题。

这包括链接到 API 参考以及概念部分！

### 保持简洁

总的来说，采取“少即是多”的方法。如果已经存在一个良好解释概念的页面，您应该链接到它，而不是重写解释，除非您记录的概念引入了一些新的细节。

保持简洁，包括在代码示例中。

### 通用风格

- 尽可能使用主动语态和现在时态。
- 使用示例和代码片段来说明概念和用法。
- 使用适当的标题级别（`#`、`##`、`###` 等）来分层组织内容。
- 使用更少的单元格但包含更多代码，以便于复制粘贴。
- 使用项目符号列表和编号列表将信息分解成易于理解的块。
- 经常使用表格（尤其是在**参考**部分）和图表来直观地呈现信息。
- 对于较长的文档页面，请包含目录以帮助读者导航内容，但对于较短的页面则隐藏。

## 设置

LangGraph 文档包含两个组件：

1. 主文档：托管在 [https://langchain-ai.github.io/langgraph/](https://langchain-ai.github.io/langgraph/)，
此综合资源是主要的面向用户文档。
它涵盖了广泛的主题，包括教程、用例、集成等，为使用 LangGraph 进行构建提供了广泛的指导。
此文档的内容位于 monorepo 的 `/docs` 目录中。
2. 代码中文档：这是代码库本身的文档，也用于生成面向外部的 [API 参考](https://langchain-ai.github.io/langgraph/reference/graphs/)。
API 参考内容是通过扫描代码库中的文档字符串自动生成的。因此，我们要求开发人员对他们的代码进行良好记录。

我们感谢所有对文档的贡献，无论是修复拼写错误、添加新教程或示例，也无论是主文档还是 API 参考。

### 📜 主文档

主文档的内容位于 monorepo 的 `/docs` 目录中。

文档使用 ipython notebook (`.ipynb` 文件) 和 markdown (`.md` 文件) 的组合编写。Notebook 会被转换为 markdown，然后使用 [MkDocs](https://www.mkdocs.org/) 进行构建。

欢迎对主文档进行贡献！🥰

修改文档后：

1. 运行 linting 和格式化命令（请参阅下文），以确保文档格式良好且没有错误。
2. 可选地，本地构建文档以验证更改的外观。
3. 提交包含更改的 pull request。

### ⚒️ 本地 Linting 和构建文档

编写完文档后，您可能希望在本地进行 linting 和构建，以确保其外观良好且没有错误。

如果您无法在本地构建，也没关系，因为您可以在 pull request 页面上看到文档的预览。

从 **monorepo 根目录**运行以下命令来安装依赖项：

<!-- TODO -->
```bash
poetry install --with docs --no-root
```

#### 构建

构建文档的代码位于 monorepo 的 `/docs` 目录中。

在构建文档之前，最好先清理构建目录：

```bash
make clean-docs
```

您可以通过以下方式构建和预览文档：

```bash
make serve-docs
```

#### Linting

要进行文档拼写检查，请在 `docs` 目录中运行以下命令：

```bash
codespell --skip="*.ambr,*.lock,*.ipynb,*.yaml,*.zlib,*.css.map,*.js.map" --ignore-words-list="infor,thead,stdio,nd,jupyter,lets,lite,uis,deque" .
```

### ️代码中文档

代码中文档是从文档字符串自动生成的。

为了使 API 参考有用，代码库必须有良好的文档记录。这意味着所有函数、类和方法都应该有一个文档字符串，解释它们的功能、参数以及返回值。这通常是好的做法，但对于 LangGraph 尤其重要，因为 API 参考是开发人员了解如何使用代码库的主要资源。

我们通常遵循 [Google Python 风格指南](https://google.github.io/styleguide/pyguide.html#38-comments-and-docstrings) 来编写文档字符串。

下面是一个文档记录良好的函数的示例：

```python

def my_function(arg1: int, arg2: str) -> float:
    """这是关于该函数的简短描述。（应为一句完整的话。）

    这是关于该函数的详细描述。它应该解释
    函数的功能、参数以及返回值。
    它应该在 88 个字符处换行。

    示例：
        这里是函数使用示例的部分。

        ```python
        my_function(1, "hello")
        \```

    Args:
        arg1: 这是 arg1 的描述。我们不需要指定类型，因为
            它已经在函数签名中指定了。
        arg2: 这是 arg2 的描述。

    Returns:
        这是返回值的描述。
    """
    return 3.14
```